# Scope Memo: Larger Local Models Evaluation (v0.4.0)

**Date**: 2026-05-04  
**Author**: Claude (PM); Opus 4.7  
**Status**: Decisions made; runs after llama.cpp backend lands

---

## Problem

Current bridge tier ceiling is `qwen2.5:7b` (Tier C). Real-world frontier-token
savings during v0.2.0 were < 5% (per CLAUDE.md). Root cause: Tier C cannot handle
*hard* tasks — longer summarization, complex extraction, multi-step transforms —
so Claude does them itself.

Hypothesis: a larger local model (Tier D) raises the ceiling enough to capture
significantly more frontier work, even at higher latency.

---

## Hardware constraint

16 GB unified memory Mac (per CLAUDE.md). Current Tier C (`qwen2.5:7b` Q4_K_M
+ 32K context) consumes ~6.7 GB. That leaves ~7–8 GB headroom for OS, browser,
Claude Code, MCP processes.

| Model class | RAM @ 4-bit + 8K ctx | RAM @ 4-bit + 32K ctx | Fits? |
|---|---|---|---|
| 7B | ~5 GB | ~6.5 GB | ✅ (current Tier C) |
| 8B | ~5.5 GB | ~7 GB | ✅ |
| 14B | ~9 GB | ~11 GB | tight, OOM risk at 32K |
| 32B | ~19 GB | OOM | ❌ |
| 70B | ~40 GB | OOM | ❌ |

Tier D ceiling: **~14B at reduced context, or 8B at full context**.

---

## Candidates

**Verified current (May 2026) via web research, not hallucinated.** Original draft
listed stale models (qwen3, qwen2.5, gemma2, llama-3.3); replaced after verification.

Current top candidates for 16 GB Apple Silicon:

| Model | Size | Released | Why | Risk |
|---|---|---|---|---|
| Qwen 3.5 8B/9B | ~5.5 GB Q4 | Feb 2026 | Top AA Intelligence Index 32.4; "best all-rounder" per 2026 Mac guides; 30–40 tok/s | New model, less in-the-wild test data |
| Qwen 3 7B | ~5 GB Q4 | 2025 | HumanEval 76.0 (top under 8B); strong for `extract`/`classify` | Older than Q3.5 |
| Llama 4 Scout | TBD — needs verification of Q4 size | April 2026 | Meta's newest open weights | Size/RAM fit unconfirmed |
| Mistral Medium 3.5 | TBD | April 29, 2026 | Newest from Mistral; strong reasoning | Likely too big for 16 GB at Q4 |
| Gemma 4 (small variant) | varies | April 2, 2026 | QAT variant designed for aggressive quantization | Need exact sizes verified |
| Phi-4 14B | ~9 GB Q4 | 2025 | Strong reasoning per parameter; proven on Mac | Tight RAM at 32 K context |
| DeepSeek V4 Flash | TBD | recent | Bridge could lean on its strong extraction | Size fit unconfirmed |

### Pre-eval verification step (NEW — closes hallucination risk)

Before downloading any model, run:
```
gem "For each of: Qwen3.5-9B, Llama-4-Scout, Mistral-Medium-3.5, Gemma-4-9B,
DeepSeek-V4-Flash — give exact GGUF Q4_K_M file size in GB and Hugging Face
repo path. Reject any > 11 GB (won't fit 16 GB Mac with headroom)."
```
Build the actual download list from verified sizes, not from this memo.

### Constraint floor

Any candidate must:
- Have a Q4_K_M (or equivalent) GGUF available on Hugging Face
- Total file size ≤ 11 GB (~70 % of 16 GB unified memory, leaves OS + KV cache room)
- Native llama.cpp support (no patches required)
- Permissive license compatible with Apache-2.0 (Llama 4 community license is OK
  for our use; Gemma license needs case-by-case check; Qwen Apache-2.0 OK)

---

## Eval methodology

Build a hard-task fixture set first, then test all candidates against it.

### Hard-task fixtures (build before any model run)

5 tasks across the 4 tool types, picked from real frontier-Claude work:

1. **summarize-long**: 20 K-word research paper → 200-word abstract preserving 5 key claims
2. **summarize-long-chunked**: 60 K-token podcast transcript → structured outline
3. **classify**: ambiguous tech-vs-policy text with 6-label taxonomy
4. **extract**: nested research-paper metadata (authors, citations, methodology) — flat
   schema only since GBNF subset constraint
5. **transform**: convert messy meeting notes → action-item list with owners

For each fixture: gold reference written by Claude (Opus). Eval = quality vs gold.

### Metrics per (model × task)

- **Quality**: 1–5 score from a Claude judge pass (Sonnet 4.6 as judge, with
  the gold reference as anchor)
- **Latency**: wall-clock seconds end-to-end
- **Memory peak**: resident bytes during call (via `vm_stat` snapshots)
- **Token efficiency**: input + output tokens used

### Promotion criteria for Tier D

A candidate becomes Tier D iff it meets ALL of:
- Quality ≥ 4.0/5.0 average across the 5 fixtures
- Latency < 60 s for tasks 1, 3, 4, 5 (within MCP wall-clock budget)
- Peak RAM < 12 GB (leaves 4 GB headroom)
- Strict equality on at least 3 schema-constrained extractions

---

## Implementation order

1. **First**: ship llama.cpp backend (separate scope memo). Without it, this eval
   is pointless — Ollama daemon would still be the dependency.
2. **Then**: build hard-task fixture set + judge harness in `tests/eval/`
3. **Then**: run eval matrix, write up results in `docs/notes/v0.4.0-eval-results-*.md`
4. **Decide**: which (if any) candidate gets promoted to Tier D
5. **Update**: `CLAUDE.md` tier table and `backendForTool()` if Tier D added

---

## Out of scope

- Cloud models (defeats privacy/local-first goal)
- 32B+ models (16 GB hardware can't run them)
- Quality < Q4_K_M quantization (degrades too much)
- New MCP tools (this memo is purely about replacing existing tier ceiling)

---

## Definition of done

- 5 hard-task fixtures committed under `tests/eval/fixtures/`
- Judge harness committed under `tests/eval/judge.mjs`
- Eval matrix run, results in `docs/notes/`
- Tier table in CLAUDE.md updated (Tier D added or explicitly rejected)
