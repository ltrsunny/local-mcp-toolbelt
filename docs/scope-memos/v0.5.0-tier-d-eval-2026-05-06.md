# Scope Memo: Tier-D Model + Backend Evaluation (v0.5.0)

**Date**: 2026-05-06
**Author**: Claude (PM, sonnet-4-6) — synthesizing 3-round multi-AI brainstorm
**Status**: Draft 2 — pending User Auditor (you)
**Supersedes**: `v0.4.0-larger-models-eval-2026-05-04.md` and Draft 1 of this memo

---

## What changed since Draft 1

**User constraint clarification (2026-05-06)**: macOS-only is acceptable for v0.5.0. This invalidates Draft 1's strongest argument against MLX (cross-platform regression).

**Multi-AI brainstorm executed (3 rounds)**:
- **Round 1**: 5 independent verdicts (Claude / Gemini / Copilot / Mistral-large-3 / Nemotron-49b)
- **Round 2**: mutual challenge — Gemini flipped to MLX with the math argument that 14B/GGUF will exceed 60s wall
- **Round 3** (transcript-mode meeting): 8 frontier-class voices including DeepSeek-V4-Pro + Kimi-K2.6 + Llama-3.1-405B, each reading prior turns before speaking

Result: Draft 1's "stay llama.cpp, reject MLX" position is replaced by a **3-pronged backend strategy**.

---

## Decisions

### D1 — Backend strategy: 3-pronged for v0.5.0

After round-3 the 8-voice meeting converged on **a deliberate hedge**, not a single-path bet:

| Path | Role | Stack |
|---|---|---|
| **MLX (primary)** | Tier-D production | Python/FastAPI HTTP bridge wrapping `mlx-lm`, called from existing `LlmBackend` interface |
| **llama.cpp / Mistral-Nemo-12B** | Fallback for Python-averse users + Linux-future-proof | unchanged `node-llama-cpp` v3 backend |
| **llama.cpp speculative-decoding spike** | Co-primary candidate for v0.6.0 if it pans out | `node-llama-cpp` with Qwen3-1.8B draft + Qwen3-8B verifier |

**Justification**:
- **Gem (round 2)** + **mistral-large-3 (round 3 turn 3)**: GGUF/Metal cannot get a 14B under 60s on this hardware. Mistral's benchmark estimate: Nemo-12B GGUF=78s, Qwen3-14B-MLX=44s.
- **DeepSeek-V4-Pro (turn 6)**: Mistral's 44s number lacks methodology disclosure; real end-to-end with FastAPI HTTP framing is likely 55-62s. Margin is thin.
- **DeepSeek + Kimi-K2.6 (turn 7)**: speculative-decoding hybrid (Qwen3-1.8B draft + Qwen3-8B verifier, 6.2 GB total, same tokenizer/architecture) could 2-3× decode on llama.cpp without any backend swap. Unproven but architecturally elegant.
- **Llama-3.1-405B (turn 8)**: 3-pronged is "calculated sprawl" justified by zero headroom in the 60s wall — if any single path fails benchmarks, project still ships.

### D2 — Tier-D primary candidate: Qwen3-14B-MLX-4bit

- mlx-community / Qwen3-14B-4bit (verify availability + size before download per D6)
- Apache-2.0 license inherited from upstream Qwen3
- Run via FastAPI HTTP bridge (see D3)

### D3 — MLX integration: FastAPI HTTP bridge (NOT Python subprocess, NOT native bindings)

- **Reject Python subprocess**: cold-start latency unacceptable for per-call MCP tool invocation; brittle stdin/stdout JSON-RPC framing for streaming tokens
- **Reject native MLX bindings from Node**: experimental, segfault risk in MCP host process
- **Accept FastAPI HTTP bridge**: isolates Python crashes, OpenAI-compatible endpoint, lifecycle managed independently, lets us iterate on MLX without recompiling Node binary

`LlmBackend` adds a third implementation `MlxHttpBackend` calling `localhost:<port>/v1/chat/completions`. `backendForTool()` resolves Tier-D to MLX-bridge by default with a fallback chain.

### D4 — License policy: Apache-2.0 / MIT / BSD only (unchanged from Draft 1)

5/5 round-1 reviewers + 8/8 round-3 voices unanimous. Reject:
- `Jackrong/Qwen3.5-9B-DeepSeek-V4-Flash-GGUF` (frankenmerge, claimed Apache but no upstream)
- `RichardErkhov/saiga_gemma_9b-gguf` (no license file, Gemma upstream is restrictive)
- Any community conversion without verifiable LICENSE matching upstream's permissive terms

Llama-license / Gemma-license: gray-listed (case-by-case, not default).

### D5 — Validation requirements (NEW — added by DeepSeek + Kimi)

**Mandatory before promoting Qwen3-14B-MLX to Tier D**:
- End-to-end wall-clock measurement via `time curl` against the running FastAPI server (NOT internal mlx-lm decoder timers — they exclude HTTP framing overhead)
- 5-trial benchmark per task; report median + 95th percentile
- 95th percentile must be **<55 s** on tasks 01/03/04/05 (5 s margin under the 60 s MCP wall)
- Peak RAM (sampled via macOS `ps -p PID -o rss=` per existing harness) <12 GB

If 95th percentile is >55 s, DO NOT promote — fall back to Mistral-Nemo-12B on llama.cpp (rank 2) or trigger spec-decoding spike (D7).

### D6 — Pre-eval verification step (mandatory; carries over from Draft 1)

Before downloading any candidate run a HEAD-request sweep (use copilot `--allow-all-paths` to grant /tmp + project access):
- Confirm official upstream HF repo (e.g. `mlx-community/Qwen3-14B-4bit`)
- Confirm Content-Length and that LICENSE file matches upstream's permissive terms
- Reject any artifact >11 GB or with missing/restrictive LICENSE

### D7 — Speculative-decoding spike (parallel, not deferred)

Per Kimi (turn 7) and Llama-3.1-405B (turn 8): run this spike **in the same v0.5.0 cycle**, not in v0.6.0.

- Pair: **Qwen3-1.8B-Instruct** (draft, ~1.1 GB Q4_K_M) + **Qwen3-8B-Instruct** (verifier, ~5.1 GB Q4_K_M) — total ~6.2 GB, same tokenizer/architecture (mandatory for speculative decoding to work)
- Stack: `node-llama-cpp` (does it support speculative decoding API? — verify in spike)
- Goal: 2-3× decode throughput on Qwen3-8B verifier → 118 s task 05 drops to 40-59 s
- Outcome:
  - If <55 s 95p → promote to **co-primary** for v0.5.0 ship; document as "no Python dep" path
  - If 55-60 s → keep as v0.6.0 candidate, document
  - If >60 s → drop, document why

---

## Candidate ladder (revised)

| Rank | Model | Backend | License | Approx size | Verified? |
|---|---|---|---|---|---|
| 1 | **Qwen3-14B-MLX-4bit** | MLX (FastAPI bridge) | Apache-2.0 | ~8 GB | Needs HEAD verify on `mlx-community` |
| 2 | **Mistral-Nemo-12B-Instruct Q4_K_M** | llama.cpp (existing) | Apache-2.0 | ~7.5 GB | Needs HEAD verify on `mistralai/...` |
| 3 (spike) | **Qwen3-1.8B + Qwen3-8B spec-decoding** | llama.cpp speculative | Apache-2.0 | ~6.2 GB total | Needs HEAD verify + node-llama-cpp spec-decode API |
| 4 | **Qwen2.5-14B-Instruct GGUF** | llama.cpp | Apache-2.0 | ~9 GB | Needs HEAD verify (older Qwen, last-resort) |
| 5 | **Granite-3-8B-Instruct** | llama.cpp | Apache-2.0 | ~5 GB | Needs HEAD verify (same size class as failed Qwen3-8B; very low priority) |

Reject (from Draft 1, confirmed by meeting): Jackrong's Qwen3.5-9B hybrid, RichardErkhov's Gemma 9B.

---

## What carries over from harness work (already done)

- **5 fixtures** committed under `packages/core/tests/eval/fixtures/`: summarize-long, summarize-long-chunked, classify, extract, transform
- **Eval harness**: runner.mjs, judge.mjs (NIM-backed via `qwen/qwen3-coder-480b-a35b-instruct` or fallback per `NV_JUDGE_MODEL`), report.mjs, ram.mjs, results.mjs, validate-fixture.mjs
- **Promotion thresholds** (unchanged): quality avg ≥4.0/5 across 5 fixtures, latency 95p <55 s on tasks 01/03/04/05 (was <60 s in Draft 1 — tightened to leave headroom), peak RAM <12 GB
- **Run JSONL schema** + idempotent judge re-runs

---

## Implementation order

1. **HEAD-verify Qwen3-14B-MLX-4bit + Mistral-Nemo-12B + Qwen3-1.8B + Qwen3-8B + Qwen2.5-14B-Instruct GGUF**. Use copilot `--allow-all-paths`.
2. **Build MLX HTTP bridge** (FastAPI, mlx-lm, OpenAI-compatible endpoint). New `MlxHttpBackend` impl behind existing `LlmBackend` interface.
3. **Download Qwen3-14B-MLX-4bit**. Run eval matrix. Report 5-trial 95p latency end-to-end.
4. **In parallel**: download Mistral-Nemo-12B + Qwen3-{1.8B,8B}. Run llama.cpp spec-decoding spike if `node-llama-cpp` supports it (verify API).
5. **Decide**: which of the 3 paths passes? Promote primary; document other 2 as fallback / experimental.
6. **Update CLAUDE.md** tier table + `backendForTool()`.
7. **Write up results** in `docs/notes/v0.5.0-tier-d-eval-results-*.md` with all 3 paths' data.

---

## Out of scope (explicit)

- Cloud models (privacy/local-first goal)
- Non-Apache/MIT/BSD models (license policy D4)
- 32B+ models (16 GB hardware can't run, even with MLX)
- Quantization < Q4_K_M / Q4_MLX (degrades too much)
- New MCP tools
- Bringing back the `ollama` daemon dependency
- Linux/Windows backend support for v0.5.0 (deferred per user constraint; revisit when there's user demand)

---

## Risk register

| Risk | Likelihood | Impact | Mitigation |
|---|---|---|---|
| Mistral's 44s MLX number is optimistic; real e2e is 55-62s with thin margin | Medium-High | High | D5 mandatory end-to-end benchmark; reject if 95p >55s |
| Python/MLX bridge lifecycle bugs (port conflicts, OOM, zombie processes) | Medium | Medium | FastAPI server isolation; supervised by Node MCP host with auto-restart; CI integration test |
| node-llama-cpp does NOT expose speculative-decoding API in v3.18 | Medium | Low (just drops the spike, not the project) | Verify in spike step 1; if missing, document as v0.6.0 candidate (when node-llama-cpp adds it or we patch) |
| Both MLX path AND spec-decoding spike fail latency | Low | High | Mistral-Nemo-12B llama.cpp at 78s is the absolute floor — explicitly accept it as Tier-D-fail and ship without Tier-D promotion (just doc the constraint) |
| HEAD-verified `mlx-community/Qwen3-14B-4bit` doesn't exist or has unclear license | Low-Medium | Low | D6 catches this; fall back to Qwen2.5-14B in MLX (older but established) |
| node-llama-cpp 3.18 silently breaks on macOS Metal API churn while team focuses on MLX | Medium | Medium | macOS CI smoke tests on llama.cpp path (DeepSeek's call in turn 6) |

---

## Definition of done

- HEAD-verified candidate table with sizes (D6)
- MLX HTTP bridge implemented + at least 1 candidate evaluated end-to-end with 5-trial 95p
- Speculative-decoding spike completed (pass / fail / API-unavailable, all documented)
- A decision recorded: **Tier D primary = X (backend Y); fallback = Z**
- CLAUDE.md tier table updated
- `docs/notes/v0.5.0-tier-d-eval-results-*.md` with all 3 paths' data + the meeting transcript reference

---

## Appendix: meeting record

**Round-3 transcript** (8 voices, mutual-challenge format): `/tmp/meeting-log.md` (will be archived to `docs/notes/` post-decision).

**Key flips during meeting**:
- Gemini (round 2): "MLX is catastrophically shortsighted" → "MLX is the only mathematical path to <60 s for 14B"
- Mistral-large-3 (round 3 turn 3): "MLX is a terrible idea" → "MLX's speedup is undeniable, my earlier reluctance was wrong"
- Nemotron-49b (round 3 turn 4): "dual-backend flexibility" → "ship Qwen3-14B-MLX as primary, Nemo-12B fallback"
- Claude/me (round 3 facilitator-only): Draft 1 "reject MLX as default" → Draft 2 "MLX primary, with parallel hedges"

**Voices not yet flipped or net-neutral**:
- Llama-3.3-70b (turn 5): stress-tested 3 concerns about MLX bridge; did not endorse a different path
- DeepSeek-V4-Pro (turn 6): added spec-decoding hybrid as parallel option, not as replacement
- Kimi-K2.6 (turn 7): refused both pure paths, advocated 3-pronged
- Llama-3.1-405B (turn 8): explicitly endorsed 3-pronged as "calculated sprawl"
- Nemotron-Ultra-253B (turn 9): API call failed (capacity); not heard

**Raw outputs**: `/tmp/{turn1-gem,turn2-copilot,turn3-mistral,turn4-nemotron,turn5-llama70,turn6-deepseek-v4,turn7-kimi,turn8-llama405}.md`
