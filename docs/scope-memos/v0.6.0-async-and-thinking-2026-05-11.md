# Scope memo: v0.6.0 — async polling + per-tool thinking flag

**Status:** Draft 2, Auditor pass on 5/6 questions (2026-05-15) — see §10. No code yet.
**Author:** Claude Opus 4.7
**Date:** 2026-05-15 (Draft 1 was 2026-05-11)
**Brainstorm synthesis:** [`docs/notes/v0.6.0-reframing-synthesis-2026-05-11.md`](../notes/v0.6.0-reframing-synthesis-2026-05-11.md)
**Prior art (3/3 done):**
- [`v0.6.0-v030-retrospective-2026-05-11-claude.md`](../prior-art/v0.6.0-v030-retrospective-2026-05-11-claude.md) — v0.3.0 retrospective
- [`v0.6.0-industry-rpc-2026-05-11-nvpro.md`](../prior-art/v0.6.0-industry-rpc-2026-05-11-nvpro.md) — industry RPC patterns
- [`v0.6.0-mcp-async-2026-05-11-copilot.md`](../prior-art/v0.6.0-mcp-async-2026-05-11-copilot.md) — cross-MCP async survey (5 candidates, SEP-2663 Tasks)
**Phase 1 eval baseline:** [`docs/notes/v0.5.1-phase1-eval-2026-05-11.md`](../notes/v0.5.1-phase1-eval-2026-05-11.md)
**Stability finding:** [`docs/notes/v0.5.x-omlx-stability-2026-05-11.md`](../notes/v0.5.x-omlx-stability-2026-05-11.md)

---

## 1. What this release delivers

Two coupled concerns shipped together because they unblock each other:

1. **Async polling triad** — three new MCP tools (`enqueue_job`,
   `check_progress`, `read_job_result`) that let any MCP client dispatch
   long-running work without hitting Claude Code's hardcoded 60 s MCP
   wall (Fact 1 in synthesis). Universal pattern; works on Claude
   Desktop, Cursor, Cline, OpenClaw, Zed equally.

2. **Per-tool `thinking` flag** — all existing tools (`summarize`,
   `classify`, `extract`, `transform`, `summarize-long`,
   `summarize-long-chunked`, `diff-semantic-index`) gain an optional
   `thinking: 'on'|'off'|'auto'` parameter. `'auto'` (the default)
   resolves to a server-side per-tool registry based on Phase 1 +
   thinking-eval data. Long-form thinking is what makes async useful;
   short-form thinking already fits sync.

Both concerns trace to the same insight from the R2 brainstorm: the
60 s wall is the **necessary precondition** to safely default
thinking-on, which is the **direct quality lever** Phase 1 eval data
points at. Quality is the real bottleneck (mean 2.5/5 no-think), not
latency or RAM.

## 2. Out of scope for v0.6.0

- **Filesystem-only return channel without polling** — rejected per gem
  R2: sandboxed clients (Cursor in some configs) may lack `file://`
  read access. `read_job_result` returns inline content as the primary
  path; file paths are an optional fallback for very large results.
- **`auto` as a runtime LLM-decides flag** — `'auto'` is a deterministic
  alias for the per-tool registry. Future "model decides if thinking
  helps" is a v0.7+ research question, not v0.6.0.
- **Bash-wait pattern as a required path** — opt-in optimization for
  Claude Code only via `wait_command` field on the `enqueue_job` return.
  Cross-client correctness does NOT depend on Bash being available.
  3/4 R2 voices vote cut Bash-wait if scope pressures; we keep it as
  optimization but commit to ship working without it.
- **Migration off `wait_for_job` (v0.3.0)** — the old long-poll tool is
  deprecated but stays for one minor version. `enqueue_job` /
  `check_progress` / `read_job_result` are the canonical path in v0.6.0;
  v0.7.0 removes `wait_for_job`.
- **Server-initiated push / SSE** — explicitly out of scope. MCP
  transport is request-response; we don't extend it.
- **Tier D demotion to experimental** (Auditor Q6 → option b, 2026-05-15):
  Phase 1 showed 14B mean 2.50 ≈ 4B mean 2.54 (no-think baseline; 14B
  earns no quality premium on most tools). Thinking-mode subset gave
  14B-transform +1.0, but the sample is small. v0.6.0 demotes Tier D
  to **experimental**: it is NOT default-routed for any tool; it remains
  reachable only via explicit `toolTierMap` override + thinking-default-on.
  The promotion decision moves to a v0.7+ thinking-on full eval.

- **Migration to MCP Tasks SEP-2663 / SSE transport** — PA #3 surfaced an
  active MCP-protocol Tasks extension (`modelcontextprotocol/experimental-ext-tasks`)
  and an SSE transport project (`kEND/mcp_sse`). Our enqueue/check_progress/
  read triad maps directly onto SEP-2663's task lifecycle, but SEP-2663
  is still experimental. v0.6.0 ships our own naming and adds a forward-
  compat note in tool descriptions; rename + protocol alignment moves to
  v0.7+ when the SEP stabilizes.

## 3. New MCP tools

> **Forward-compat note (PA #3, SEP-2663, 2026-05-15)**: The triad below
> is an explicit **compatibility layer** for MCP clients that don't yet
> support the protocol-level Tasks extension. When SEP-2663 stabilizes
> AND at least one major client (Claude Code / Cursor / Claude Desktop)
> ships native `tools/call → resultType: "task"` support, the **entire
> triad deprecates** — existing tools (`summarize`, `transform`, etc.)
> will return native task descriptors directly and clients auto-poll
> via protocol-level `tasks/get`. v0.6.0 deliberately picks naming
> (`enqueue_job`, `job_id`) **distinct from** SEP's `tasks/*` / `task_id`
> namespace to avoid two-vocabulary confusion during the transition
> window in v0.7+. We are not renaming our tools to match SEP; we are
> sunsetting them.

### 3.1 `enqueue_job`

```
enqueue_job(
  tool: string,                    // any existing tool name (not enqueue_job itself)
  args: Record<string, unknown>,   // forwarded to the underlying tool
  opts?: {
    thinking?: 'on' | 'off' | 'auto'   // default 'auto' (see §4)
  },
)
→
{
  job_id: string,                  // nanoid(10), path-safe
  enqueued_at: string,             // ISO8601
  result_uri: string,              // absolute file:// URI (server expands ~ at write time),
                                   //   e.g. file:///Users/<you>/.local-mcp/jobs/<id>.json
  est_ms?: number,                 // server estimate based on input size + tool
  wait_command?: string,           // present iff client has 'bash' capability
}
```

Side effects:
- Persists `{status:'queued', tool, args, thinking_resolved, enqueued_at}`
  to `~/.local-mcp/jobs/<id>.meta.json`.
- Worker picks up job; runs underlying tool with the resolved thinking
  setting; writes result via atomic rename to `<id>.json`.
- Dedups identical concurrent enqueues by SHA-256 of canonical-sorted
  `(tool, args, thinking_resolved)`. v0.3.0 dedup logic re-used.

### 3.2 `check_progress`

```
check_progress(job_id: string)
→
{
  status: 'queued' | 'running' | 'done' | 'error',
  progress?: number,               // 0–100, only when meaningful (chunked tools)
  eta_s?: number,                  // server's remaining-time estimate
  error?: string,                  // present iff status === 'error'
}
```

Instant return (<100 ms wall). Polled by clients without Bash. Each call
is one MCP roundtrip; safe to call repeatedly. Suggested polling
cadence: every 2 s for the first 60 s, every 5 s thereafter.

### 3.3 `read_job_result`

```
read_job_result(job_id: string)
→
{ content: string }                // inline result text, ≤ 1 MB default
| { file_path: string,             // for results > 1 MB; client reads via file:// or its own means
    bytes: number }
| { error: string }
```

Inline by default. Threshold configurable via `OMCP_INLINE_RESULT_MAX_BYTES`
(default 1 048 576). Above the threshold, the bridge persists to disk and
returns the path — but most bridge tasks (summary, classify, extract)
produce ≤ 50 KB output, so file path is rarely used.

## 4. Modified existing tools — `thinking` flag

All seven existing tools (`summarize`, `summarize-long`,
`summarize-long-chunked`, `classify`, `extract`, `transform`,
`diff-semantic-index`) gain:

```
  thinking?: 'on' | 'off' | 'auto'   // default 'auto'
```

Resolution order:
1. Per-call value passed to the tool overrides everything.
2. Per-tool env var `OMCP_THINKING_<TOOL_NAME>` (e.g.
   `OMCP_THINKING_SUMMARIZE=off`) — uppercased + underscored.
3. Global env var `OMCP_THINKING_MODE=on|off` — affects all tools.
4. Tool-registry default in `server.ts`:

| Tool | Default | Justification (Phase 1 + thinking-eval) |
|---|---|---|
| `classify` | on | structured output; reasoning helps decide |
| `extract` | on | schema-bound; reasoning helps comply |
| `transform` | on | thinking-eval transform +1.0 score lift |
| `diff-semantic-index` | on | structured + reasoning task |
| `summarize` | off | prose; thinking-eval summarize −0.5 score loss |
| `summarize-long` | off | same |
| `summarize-long-chunked` | off | same |

Implementation: `MlxHttpBackend.chat()` already supports
`OMCP_THINKING_MODE=on` (env var added in commit pending in v0.5.1's
session). v0.6.0 extends to per-tool granularity by computing the
effective value in `src/mcp/server.ts` per tool call and passing it
into ChatOptions as a new field `disableThinking: boolean`. The
backend honors that field and appends/omits the `/no_think` suffix
accordingly.

## 5. Cross-client policy

| Client | Behavior |
|---|---|
| Claude Code | Use `enqueue_job` for any task expected to exceed 60 s. Use `wait_command` (returned by `enqueue_job`) via the built-in Bash tool to collapse poll+read into one Bash invocation. |
| Claude Desktop | 240 s request timeout. May use sync tools for most cases. For >240 s tasks, `enqueue_job` + poll `check_progress` works without Bash. |
| Cursor / Cline / OpenClaw / Zed | Configurable timeouts; same async triad as Claude Desktop. No Bash dependency. |

Capability detection: `wait_command` is returned ONLY when the client
advertises `bash` capability (MCP capability handshake) OR the env var
`OMCP_ASSUME_BASH_CLIENT=1` is set. Defaults: no advertisement → no
`wait_command` field in response.

## 6. Engineering plan

**Reusable from existing code:**
- v0.3.0 `JobRegistry`, `JobStore`, `JobRunner`, `progress-capture` —
  the queue + persistence + worker machinery is intact.
- v0.5.1 `MlxHttpBackend` circuit-breaker + telemetry — masks oMLX
  abort within both sync and async paths.
- v0.5.1 `OMCP_THINKING_MODE` env var — already wired; per-tool
  extension is incremental.

**New work, sized:**
- `src/mcp/tools/enqueue-job.ts` v2 (replace v0.3.0 in same path) — ~80 LOC
- `src/mcp/tools/check-progress.ts` — ~50 LOC
- `src/mcp/tools/read-job-result.ts` v2 (inline-content default) — ~60 LOC
- `src/config/thinking-defaults.ts` — per-tool registry + env var
  resolution — ~50 LOC
- Modify `src/mcp/server.ts` — wire thinking resolution into each tool
  handler, pass to `ChatOptions.disableThinking` — ~30 LOC
- Modify `src/llm/mlx-http-backend.ts` — add `disableThinking?: boolean`
  to `ChatOptions`, honor it alongside env var — ~10 LOC
- `wait_command` string generation + capability check — ~30 LOC
- Tests: ~150 LOC across ~12 new unit cases
- Docs: README + CLAUDE.md (under 200 lines) + CHANGELOG — ~80 lines

**Total: ~310 LOC source + ~150 LOC tests + ~80 lines docs.**
Sprint-sized; ~3-5 dev days at v0.3.0 pace.

**Order:**
1. Add `disableThinking` to `ChatOptions` + `MlxHttpBackend` honor it.
   Per-tool thinking registry. Tests. (Day 1)
2. New tool: `enqueue_job` v2 with `wait_command` + capability detection.
   Tests. (Day 2)
3. New tool: `check_progress`. Tests. (Day 2-3)
4. New tool: `read_job_result` v2 (inline default). Tests. (Day 3)
5. Deprecate `wait_for_job` (keep but mark deprecated in tool description).
   Migration note in CHANGELOG. (Day 4)
6. Re-run Phase 1 eval with thinking-on registry defaults; document
   real quality lift; update README. (Day 4-5)
7. Cross-client smoke: Claude Code + Claude Desktop minimal test.

## 7. Success criteria (measurable)

1. **Quality lift demonstrated** (Auditor Q5: keep ≥ 4.0/5 target,
   2026-05-15): re-run of Phase 1 eval (5 fixtures × 3 trials × 2 tiers
   B+C = 30 trials; Tier D is experimental and not part of this criterion)
   with v0.6.0 defaults produces:
   - Mean score **≥ 4.0/5** on at least 1 of the 2 tiers (B or C)
   - Transform task specifically: **≥ 4.0/5** on Tier C with thinking-on
   - Summarize tasks: no regression vs Phase 1 baseline (no-think) — the
     per-tool default off must protect prose quality.
   - Tier D, if anyone re-runs eval with it explicitly enabled, is **not
     subject** to the ≥ 4.0 bar in v0.6.0; experimental-status disclaimer.
2. **Cross-client smoke on Claude Desktop** (Auditor Q4 → option a,
   2026-05-15): `enqueue_job` → `check_progress` → `read_job_result`
   triad works against Claude Desktop (config swap, 240 s timeout
   advantage). At minimum: one `summarize` (sync) + one `transform`
   (thinking-on, async via enqueue_job) per release. Cursor/Cline/Zed
   support is not gated on this; only Claude Desktop is required.
3. **No user-visible failures from oMLX abort**: 100 sequential
   thinking-mode trials across the eval matrix; circuit-breaker telemetry
   (commit 994f01c) shows ≤ 5 % retry rate; all 100 trials return a final
   result (retry-masked or first-try-ok).
4. **60 s wall not binding**: ≥ 90 % of thinking-on calls in the eval
   matrix complete within **60 s of `enqueue_job`** — i.e. one
   `check_progress` call suffices and `read_job_result` follows
   immediately. The remaining ≤ 10 % use the multi-poll async-job
   pattern.
5. **≥ 160 unit tests pass** (v0.5.1 baseline is 152; v0.6.0 adds
   roughly 12 tests across the 3 new tools + thinking-flag resolution
   + the per-tool registry).

## 8. Risks + mitigations

| Risk | Mitigation |
|---|---|
| `'auto'` interpretation drifts toward "model decides" | Locked in §2 as alias for registry; no runtime LLM decision in v0.6.0 |
| Sandboxed clients lack file:// access | `read_job_result` returns inline content as primary; large results spill to file with explicit threshold |
| Anthropic narrows Bash tool 10 min cap | `wait_command` is opt-in via capability; universal polling still works |
| oMLX SIGABRT under thinking-on (heavier decode = more Metal command buffers) | Circuit-breaker already in place; stderr telemetry monitors rate; success-criterion #3 caps retry-rate at 5 % |
| Per-tool registry defaults wrong for some inputs | Per-call override + env var override; document the resolution order |
| `wait_for_job` consumers break | Deprecation banner stays for one minor version; v0.6.0 README documents the migration |
| MCP Tasks SEP-2663 stabilizes AND a major client ships native support before our v0.7.0 | Track SEP-2663 PR + MCP SDK release notes every 2-4 weeks. If signal hits during the v0.6.0 window, scope v0.7.0 to **deprecate the entire triad** (not rename — names stay distinct from `tasks/*` to avoid migration-period confusion); existing tools start returning native task descriptors. |
| Claude Desktop smoke test fails (config / capability mismatch) | Treat as launch blocker for v0.6.0 — fix or drop wait_command / capability negotiation until it works on both clients |

## 9. Auditor questions

1. Is the **2-of-3 Prior Art Review** plus brainstorm synthesis enough
   to pass feature-intake-rule, or do you want the 3rd candidate
   (cross-MCP async pattern scan) before approving Draft 1?
2. Should `enqueue_job` accept a `priority` field (e.g. 'normal' vs
   'background') for users with many concurrent jobs? Current §3.1 omits.
3. Should we ship a `list_jobs()` tool for debug visibility, or is that
   over-surface for v0.6.0? (3/4 R2 voices didn't include it.)
4. Cross-client smoke test: which client besides Claude Code do you want
   us to actually verify? Claude Desktop is easiest; Cursor would be a
   harder cell.
5. Is the success criterion ≥ 4.0/5 too aggressive given Phase 1
   no-think baseline ~2.5? Thinking-eval subset showed transform +1.0
   max lift — that gets to 3.0. Should the bar be ≥ 3.5?
6. Tier D status: keep opt-in with thinking-default-on (current plan)
   OR aggressively demote until full thinking-on eval lands?

## 10. Sequence with v0.5.x

v0.5.1 (current main) ships independently of v0.6.0. v0.5.1's
contributions (HEAD upgrade + circuit-breaker + telemetry + Phase 1
baseline + `OMCP_THINKING_MODE` env var) all stay; v0.6.0 builds on
them. No revert of v0.5.x work is part of v0.6.0.

If v0.6.0 is abandoned mid-development, v0.5.1 still ships a
better-than-v0.5.0 product (the circuit-breaker alone is meaningful
stability gain).

---

## 11. Auditor review log

### Draft 1 → Draft 2 review (2026-05-15)

User acted as Auditor. Of the 6 questions surfaced in Draft 1 §9:

| # | Question | Answer | Draft 2 change |
|---|---|---|---|
| Q1 | Is 2/3 PA + brainstorm enough, or do we need 3rd PA? | **Need 3rd** | Commit `50fd443` adds `v0.6.0-mcp-async-2026-05-11-copilot.md` (5 candidates incl. MCP Tasks SEP-2663). PA Review now 3/3. |
| Q2 | Add `priority` field to `enqueue_job`? | (implied no; YAGNI) | Unchanged; documented in §2 as out of scope. |
| Q3 | Add `list_jobs()` tool? | (implied no; R2 majority no) | Unchanged; out of scope. |
| Q4 | Cross-client smoke target? | **(a) Claude Desktop** | §7 criterion #2 hardened: Claude Desktop is the required cross-client target, with explicit `summarize` (sync) + `transform` (async) per release. |
| Q5 | Quality bar — 4.0/5 vs 3.5/5? | **Keep 4.0/5** | §7 criterion #1 retains ≥ 4.0/5, scoped to Tier B and C (Tier D moved out of the criterion per Q6). |
| Q6 | Tier D — keep opt-in or demote? | **(b) Demote to experimental** | §2 adds explicit demotion; §7 removes Tier D from quality criterion; future promotion decision deferred to v0.7+ thinking-on full eval. |

**Net structural changes from Draft 1**:
- PA Review status flipped to 3/3 done
- Tier D moves to experimental (was opt-in default) — affects §2, §3 routing, §7 criteria, future CLAUDE.md tier table edit
- Cross-client smoke is hardened from "nice to have" to "required for Claude Desktop"
- New PA #3 surfaces MCP Tasks SEP-2663 — Draft 2 adds note in §3 and risk in §8 for forward-compat naming alignment

**Draft 2 status:** Auditor PASS on 5/6 (Q1/Q4/Q5/Q6 + Q2/Q3 by default).
Implementation can proceed under feature-intake-rule.

### Outstanding for v0.7+

Two items popped in Draft 2 review that are NOT v0.6.0 blockers:

1. **SEP-2663 native task return — deprecate the triad** (NOT rename).
   Trigger: SEP-2663 stabilizes AND ≥ 1 major MCP client (Claude Code /
   Cursor / Claude Desktop) ships native `resultType: "task"` support.
   Plan when triggered:
   - v0.7.0: keep both pathways working in parallel; existing tools
     gain ability to return native task descriptors when the client
     advertises support. Compat triad still callable.
   - v0.8.0: remove the compat triad (`enqueue_job`, `check_progress`,
     `read_job_result`) after one minor-version deprecation notice.
   Naming stays intentionally distinct (`job_*` vs SEP's `task_*`) so
   users don't conflate our compat tools with the protocol-native pathway
   during the transition.
   Cadence: re-check SEP-2663 PR + MCP SDK release notes every 2-4 weeks.
2. **Tier D promotion eval** — design and run a thinking-on full eval
   (5 fixtures × 5 trials × 14B-Qwen3 + 14B-other-family) to test
   whether 14B earns its memory cost on the current tool mix. If yes,
   promote Tier D back to opt-in default. If no, retire Tier D entirely.

### CLAUDE.md follow-up (not part of this memo)

Tier D demotion → CLAUDE.md tier table needs a "experimental" status
flag added. Separate `meta(process):` commit; not coupled to v0.6.0
implementation.
