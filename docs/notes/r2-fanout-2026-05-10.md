# R2 fan-out — strategic Q1 ordering fork (2026-05-10)

You're a fresh adversarial reviewer. Read this entire brief and pick a side.

## Project context (cold start)

`local-mcp-toolbelt` v0.5.0 — Apache-2.0 Node MCP server. **Single-user
project (rd's own machine)**, not multi-tenant. Lets MCP clients (Claude
Code) delegate bulk text tasks (summarize/extract/classify/transform) to a
local oMLX inference server, saving frontier tokens.

This session collapsed 3 backends → 1 (single MlxHttpBackend → oMLX,
serving Qwen3-4B-Instruct/8B/14B). 147 tests pass. Just shipped a
PreToolUse hook that auto-routes external file reads via bridge.

## The Q1 fork — what to do for v0.6.0

R1 votes:
- **gem (Gemini 3.1 Pro)**: (b) **eval automation** — re-run 5-trial × 4-task
  matrix, data-back the 4B-Instruct switch (which was made on 1 curl test).
- **claude (synthesizer)**: (c) **docs pass + bridge dogfood** — README,
  CHANGELOG, scope memos all lie about the new architecture.
- **copilot**: answered wrong brief (security audit), but flagged: file://
  auto-ingestion + unauthenticated oMLX = full-pipeline-compromise risk.
- **nv_pro / mistral**: 504 / errored / no R1.

R2 mutual challenge produced **partial convergence on hybrid**, but
**ordering disagreement**:

| Phase | gem's R2 ordering | claude's R2 ordering |
|-------|-------------------|----------------------|
| 1 | security fix (file:// sandbox + weight signing) | Type-A docs (architecture/CHANGELOG/delete Frankensteins, NOT config-dependent) |
| 2 | eval matrix re-run | eval matrix re-run |
| 3 | docs | Type-B docs (model recommendations, latency tables, depend on eval) |
| parallel | — | security scope memo (single-user → not blocking) |

**Key disagreement**: claude split docs into Type-A (config-independent
historical/architectural) + Type-B (config-dependent recommendations).
gem's R2 didn't make this split, treats all docs as gated on eval+security.

**Threat model assumption**: claude assumes single-user/single-machine
(no external attacker can drop /tmp files; defender layer covers
prompt-injection in source_uri content). gem assumes broader threat
surface (untrusted files could land anywhere, weights aren't verified).

**The user (project owner)**: this is rd's personal toolbelt to save
their own Claude Code tokens. NOT a published-to-strangers product
(though MIT/Apache makes it OPENABLE later). No multi-tenant deployment
planned for v0.6.0. They install via `git clone + npm install`, no
download from a public registry. They control /tmp themselves.

## Your task (R2 fresh-voice)

Pick ONE position with terse rationale (≤200 words):

- **gem-ordering**: security → eval → docs (gem is right; claude's
  Type-A/B split is sophistry; security gates everything because docs
  reference behaviour that depends on un-attested model weights).
- **claude-ordering**: Type-A docs (parallel security scope memo) →
  eval → Type-B docs (claude is right; threat model is single-user;
  docs that don't reference per-tier models can ship now without eval).
- **third-way**: name your different ordering with explicit phase
  contents.

End with one-line verdict label. End with one critique of EITHER position
that both gem and claude missed.

≤200 words total. Argue, don't summarize.
