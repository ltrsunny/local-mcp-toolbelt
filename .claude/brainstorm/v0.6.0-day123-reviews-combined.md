# 3-AI adversarial review of v0.6.0 Day 1-3
## gem (gemini-3.1-pro-preview, --strict)
1. RACE CONDITIONS / CONCURRENCY (≤120 words)
In `runner.ts` (lines 86-89), `job.args` is passed by reference if `thinking_resolved === undefined`, and only shallow-copied via spread (`{ ...job.args }`) otherwise. If the wrapped tool mutates nested objects within `args` (e.g., JSON schemas or arrays), it permanently corrupts the in-memory cached job state. Concurrent jobs sharing the same underlying args, or a retried job reusing the metadata, will see corrupted input. This is not thread-safe. A deep clone is required across the invocation boundary.

2. DEDUP HASH SOUNDNESS (≤80 words)
The hash shape is perfectly backward-compatible (`registry.ts`, lines 216-224). By conditionally appending the `th` key only when `thinking_resolved !== undefined`, a legacy v0.3.0 job loaded from disk (which lacks this field) evaluates to exactly `{ t, a }`, matching the old `hashRequest` schema. `initialize()` will ingest old jobs cleanly.

3. FORWARD-COMPAT WITH MCP TASKS SEP-2663 (≤80 words)
The separation of concerns is clean. The `thinking` parameter is injected directly into the core synchronous tool inputs (`server.ts`, lines 311, 399, etc.). When SEP-2663 arrives, the async triad tools (`enqueue_job`, etc.) and `JobMetadata` schema can be deleted entirely. The core synchronous tools will plug directly into the native task system without modification, as they already accept `thinking`.

4. EDGE CASES + MISSING TEST COVERAGE (≤120 words)
- **`read_job_result` threshold:** "0" or "-1" safely fallback to 1 MB because `parsed > 0` evaluates false (`server.ts`, line 1229).
- **`check_progress`:** If `current > total`, the formula yields >100% (`server.ts`, line 1297). It is missing a clamp (e.g., `Math.min(current, total)`).
- **`wait_command`:** Critical bash escaping bug. Paths are unquoted: `[ ! -f ${resultPath} ]` and `cat ${resultPath}` (`server.ts`, line 1448). If `resultPath` contains spaces, the bash loop throws a syntax error and `cat` breaks. Must be `"${resultPath}"`.
- **Tier D:** Not addressed in this diff; if a fallback hits Tier D, we rely on the unmanaged backend defaults.

5. API DESIGN — `thinking: 'auto'` SEMANTICS (≤80 words)
The code correctly enforces 'auto' as an ephemeral input alias. `resolveThinking` explicitly checks for `'on' | 'off'` and falls back to env vars or defaults, guaranteeing a strict binary return (`thinking-defaults.ts`, lines 96-107). Furthermore, `thinking_resolved` in the `enqueue_job` response (`server.ts`, line 1459) and `JobMetadata` type (`store.ts`, line 69) is typed strictly as `'on' | 'off'`. A caller cannot observe 'auto' as runtime state.

6. ONE STEELMAN — "WHAT WOULD MAKE YOU REVERT DAY 1-3?" (≤40 words)
The unquoted `${resultPath}` in `wait_command` (`server.ts`, line 1448) breaks execution for any workspace path with spaces, and the `args` pass-by-reference mutation risk in `runner.ts` (line 89) risks unpredictable job corruption. Both justify a fix before Day 4.

## copilot (--effort xhigh --yolo)
1) RACE CONDITIONS / CONCURRENCY (≤120 words)
- Problem: runner injects into args via a shallow copy: packages/core/src/jobs/runner.ts @@ -78,7 +78,15 @@ — argsForInvoke = job.thinking_resolved !== undefined ? { ...job.args, thinking: job.thinking_resolved } : job.args;
- Risk: shared nested objects remain shared; if the wrapped invoker mutates args (or other code reuses the same args object across enqueues), concurrent runners (tests can raise concurrency) can observe racey modifications. Also hash/dedup uses sortKeys(args) (registry.hashRequest, packages/core/src/jobs/registry.ts @@ -159,7 +164,8 @@) — if args mutate between enqueue and hashing/read, dedup correctness breaks.
- Recommendation: deep-freeze or deep-clone args at enqueue and before invoke (or document immutability requirement), and avoid relying on shallow copies.

2) DEDUP HASH SOUNDNESS (≤80 words)
- Change: hashRequest adds thinking_resolved to canonical form (packages/core/src/jobs/registry.ts @@ -159,+).
- Compatibility risk: older persisted metadata files lack th/thinking_resolved. On restart, registry.initialize() must reconstruct inflightHashes using the old-shape metadata; diff shows enqueue writes thinking_resolved conditionally (@@ -110,+). If initialize doesn't compute the legacy hash variant, previously-queued jobs could be re-enqueued as duplicates. Add startup migration: compute both old and new hashes when indexing existing metadata.

3) FORWARD-COMPAT WITH MCP TASKS SEP-2663 (≤80 words)
- Coupling: public types and persistence now include ThinkingMode/ThinkingInputSchema (src/config/thinking-defaults.ts) and JobMetadata.thinking_resolved (store.ts). These are baked into persisted files and tool schemas (mcp/server.ts additions).
- Migration friction: SEP-2663 adoption will require stripping persisted thinking_resolved and changing APIs; current changes increase migration surface. Suggest: mark persisted key optional with explicit migration helpers and keep a small feature-flag toggle to disable triad plumbing for easier rollback.

4) EDGE CASES + MISSING TEST COVERAGE (≤120 words)
- OMCP_INLINE_RESULT_MAX_BYTES parsing (packages/core/src/mcp/server.ts @@ -1194,+): raw "0" or "-1" -> parsed = 0 or -1 -> code falls back to default 1_048_576 (because parsed > 0 required). Ambiguity: operator may want "0" to mean "always file" or "unlimited". Add explicit handling and document semantics; add tests for "0", "-1", "1e6".
- check_progress percent (mcp/server.ts @@ +1364...): computes Math.round((current/total)*100) without clamping; if current>total yields >100 or negative if current<0. Add clamp to 0..100 and tests for boundary conditions.
- wait_command escaping (enqueue_job, mcp/server.ts @@ +1364...): builds: while [ ! -f ${resultPath} ]; do sleep 5; done; cat ${resultPath} — UNQUOTED. If resultPath contains spaces, glob chars, or shell metacharacters this breaks or allows command injection. Must quote/escape path (printf %q or wrap in single-quotes with proper sanitisation) and/or avoid returning shell one-liners (return array form or a safe helper).
- Tier D routing: CLAUDE.md mentions Tier D, but code changes don't demote/route tiers; add tests ensuring no auto-routing to experimental D tier and audit config.tiers resolution for missing overrides.

5) API DESIGN — `thinking: 'auto'` SEMANTICS (≤80 words)
- Code matches doc: resolveThinking() (src/config/thinking-defaults.ts) resolves 'auto' → concrete 'on'|'off' before persisting and runner injects only resolved values (runner.ts @@ -78...). However public types still expose 'auto' in schemas; callers may incorrectly assume it's a dynamic runtime LLM decision. Recommend explicit API doc string and server-side rejection/test ensuring no persisted 'auto' sneaks into metadata.

6) ONE STEELMAN — "WHAT WOULD MAKE YOU REVERT DAY 1-3?" (≤40 words)
- Revert if wait_command returns an unescaped shell string that can be influenced by resultPath (packages/core/src/mcp/server.ts @@ +1364...), introducing a trivial command-injection vector — high-severity and easy to exploit across clients.


Changes   +0 -0
Requests  1 Premium (22s)
Tokens    ↑ 28.2k • ↓ 2.4k • 2.6k (cached) • 1.4k (reasoning)

## nv_pro (Qwen3.5-397b-a17b via NIM)
### 1. RACE CONDITIONS / CONCURRENCY
The `JobRunner` creates a shallow copy of `args` (`argsForInvoke`) to inject `thinking_resolved` (runner.ts:85-89). While this prevents direct mutation of the persisted `job.args`, a race condition exists if the wrapped tool performs asynchronous operations on the passed object *after* the runner proceeds but *before* the job completes. More critically, `JobRegistry` relies on `inflightHashes` for deduplication (registry.ts:96-107). If `enqueue_job` is called concurrently with identical args before the first call writes to disk and sets the hash, both might pass the `existingId` check if the `inflightHashes` map isn't updated atomically with the write. The code updates `inflightHashes` at line 124, but if two requests hit line 96 simultaneously before either reaches 124, duplicate jobs could spawn. The `active` map write (line 125) is also not atomic with the hash map update.

### 2. DEDUP HASH SOUNDNESS
The `hashRequest` function now includes `thinking_resolved` (registry.ts:216-223). This breaks backward compatibility for any persisted job metadata files from v0.3.0 that lack this field. When `registry.initialize()` loads these older JSON files, `thinking_resolved` will be `undefined`. The new hash logic treats `undefined` differently than explicit `'on'`/`'off'` (due to the `...(thinking_resolved !== undefined ...)` spread in line 222). Consequently, restarting the server with old job files may cause the system to treat resumed jobs as distinct from their original enqueue signatures, potentially bypassing deduplication checks or causing hash mismatches if a client retries an old job ID expecting the old behavior.

### 3. FORWARD-COMPAT WITH MCP TASKS SEP-2663
The current implementation tightly couples the async triad (`enqueue_job`, `check_progress`, `read_job_result`) to specific file-system paths and internal `JobMetadata` structures (store.ts:59-74). If SEP-2663 introduces a standardized task state machine, migrating away from the current `file://` result URIs and custom polling logic (`check_progress` parsing local JSON) will require significant refactoring. Specifically, the `wait_command` generation (server.ts:1394) hardcodes a POSIX loop dependent on file existence, creating a "compatibility trap" where clients rely on this specific shell implementation rather than a standardized task API, making the eventual deprecation of the triad disruptive to user workflows.

### 4. EDGE CASES + MISSING TEST COVERAGE
- **Env Var Parsing**: In `server.ts` (lines 1225-1228), `OMCP_INLINE_RESULT_MAX_BYTES` parses via `Number()`. If the env var is `"0"`, `"-1"`, or `"abc"`, the logic defaults to 1MB. However, `"0"` results in `parsed > 0` being false, correctly defaulting, but explicit `"0"` intent (force file return) is lost.
- **Progress Calculation**: `check_progress` (server.ts:1436) guards against `total === 0`, but if `current > total` (a possible race or bug in chunking), the percentage exceeds 100%. No clamp is applied.
- **Shell Injection**: The `wait_command` (server.ts:1394) interpolates `resultPath` directly into a shell string. If `resultPath` contains spaces, backticks, or `$(...)`, the command breaks or becomes vulnerable. It lacks proper escaping (e.g., `shellescape`).
- **Tier D**: The code still references `tierForTool` (server.ts:625) without explicit demotion logic for Tier D tools in the async path, potentially allowing experimental tools to bypass intended restrictions if added to the whitelist.

### 5. API DESIGN — `thinking: 'auto'` SEMANTICS
The code correctly implements `'auto'` as a deterministic alias for registry defaults (thinking-defaults.ts:108-110), not a runtime LLM decision. However, the return value of `enqueue_job` includes `thinking_resolved` (server.ts:1407). If a user passes `'auto'`, they receive `'on'` or `'off'` back. This is good transparency, but the term `'auto'` in the input schema might mislead users into expecting dynamic adaptation per-token, whereas it is purely a static configuration lookup. The documentation in `ThinkingInputSchema` (thinking-defaults.ts:66-72) is clear, but the cognitive load of distinguishing "auto" (static fallback) from "dynamic" remains a potential UX trap.

### 6. ONE STEELMAN — "WHAT WOULD MAKE YOU REVERT DAY 1-3?"
The unescaped shell interpolation in `wait_command` (server.ts:1394) is a critical security and stability flaw. If a job result path contains special characters (common in temp directories or user-named paths), the generated command fails or executes arbitrary code. This single issue justifies a revert to prevent potential command injection or broken workflows in heterogeneous environments.
