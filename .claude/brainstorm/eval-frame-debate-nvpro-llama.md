The assumption that v0.6.0 must rerun Phase 1 eval before release is based on a carryover from v0.5.0's scope memo, which was not re-audited for v0.6.0's specific changes. However, upon closer examination, it becomes clear that the changes in v0.6.0 are primarily behavioural and protocol-related, with the introduction of thinking-mode defaults and async-job triad. These changes are already covered by unit tests, and the thinking-mode defaults are behaviourally observable.

The Phase 1 design, which consists of 5 fixtures × 5 trials × LLM-as-judge × ≥ 4.0/5 mean score, may not be the most effective validation mechanism for v0.6.0. The use of LLM-as-judge introduces potential biases and limitations, particularly with the introduction of self-family bias with `nv_pro` and a new baseline with `gem`. Furthermore, the success anchor is based on comparability to the v0.5.1 baseline, which reported 3/3 tiers FAIL, indicating potential miscalibration in the design or bar.

Given these considerations, it is reasonable to question whether v0.6.0 should ship without a Phase 1 rerun at all. Alternative validation mechanisms, such as programmatic gold-match on structured tasks, dogfood-based release gate, or shipping on unit tests + behavioural assertions, may be more effective and efficient. These approaches can provide more probative results, especially for tasks that can be structurally compared, such as classify and extract.

The constraint of no budget outside existing subscriptions rules out Anthropic API, but `nv_pro` and `gem` are still options. However, considering the potential biases and limitations of these options, it may be more prudent to explore alternative validation mechanisms that do not rely on LLM-as-judge.

In conclusion, I recommend that v0.6.0 ship without a Phase 1 rerun, and instead, use a combination of programmatic gold-match on structured tasks and dogfood-based release gate as the alternative validation mechanism. This approach can provide more effective and efficient validation, while also addressing the potential biases and limitations of the original Phase 1 design. Specifically, I recommend option **F. Programmatic gold-match on structured tasks + spot-check on prose tasks**, which can provide a more robust and reliable validation mechanism for v0.6.0.
